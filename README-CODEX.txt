VITEC SCOREBOARD - CODEX HANDOFF v0.8.10

Primary instructions:
  CODEX-HANDOFF.md

First run:
  VERIFY-BASELINE-BUILD.cmd

Known-safe publish reference:
  installer\Publish-Defender-Safe.cmd

Current important known issue:
  PostgreSQL test machine has no reachable listener on 127.0.0.1:5432.

Do not use older v0.7.x / experimental branches as the baseline.
